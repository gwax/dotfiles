# video quality: best but not larger than 1080p
-f bestvideo[height<=?1080]+bestaudio/best

# aria2 downloader configuration
--external-downloader aria2c
--external-downloader-args "-c -j 7 -x 7 -s 7 -k 1M"

# output format
--output "[%(playlist_index)03d][%(id)s] %(title)s.%(ext)s"

